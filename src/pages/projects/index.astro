---
import Base from '../../layouts/Base.astro';
---

<Base title="花艺课程表海报自动生成工具">
  <article class="container" data-fade>
    <header class="project-header">
      <p class="block-eyebrow">Tool · 2026</p>
      <h1 class="project-title">
        花艺课程表海报，从 4 小时缩到 1 分钟。
      </h1>
      <p class="project-sub">
        为有花可期工作室做的一张小工具。改一处，整张海报自动跟着变。
      </p>
    </header>

    <section class="project-section" data-fade>
      <p class="block-eyebrow">问题</p>
      <p>
        工作室有两套花艺课程：零基础创业班和创业全能班。每次开新课，都要重做两张课程表海报发出去。
      </p>
      <p>
        之前用 PPT 或 Canva 做，一张海报 2-4 小时，两张就是半天。更麻烦的是每张海报里有 30 多处日期、5-6 个章节标题、100 多个技能点，改一处常常忘了另一处。
      </p>
    </section>

    <section class="project-section" data-fade>
      <p class="block-eyebrow">做法</p>
      <p>
        把“内容”和“版式”分开。版式定好一次，以后只改数据。
      </p>
      <p>
        运营改一个开课日期，全表 30 多处日期一起更新；改一个章节名，罗马序号自动重排；两套课程共用一份数据源，重复内容不用抄两遍。
      </p>
    </section>

    <section class="project-section" data-fade>
      <p class="block-eyebrow">结果</p>
      <p>两张海报从 4-8 小时变成 1 分钟。保存即可发。</p>

      <figure class="project-figure">
        <img
          src="/projects/floral-schedule/01-zero-baseline.jpg"
          alt="零基础创业班课程表海报"
          loading="lazy"
        />
        <figcaption>零基础创业班 · 10 天 / 5 章 / 43+ 课</figcaption>
      </figure>

      <figure class="project-figure">
        <img
          src="/projects/floral-schedule/02-all-in-one.jpg"
          alt="创业全能班课程表海报"
          loading="lazy"
        />
        <figcaption>创业全能班 · 12 天 / 6 章 / 55+ 课 · 花艺 + 气球</figcaption>
      </figure>
    </section>

    <section class="project-section" data-fade>
      <p class="block-eyebrow">下一步</p>
      <p>
        现在它只是一个内部工具，不是产品。我还在想：这种“重复劳动自动化”的小工具，能不能做成让更多花艺、教培、餐饮商家都能用的东西？
      </p>
    </section>
  </article>
</Base>

<style>
  .project-header {
    padding-block: var(--space-8) var(--space-12);
  }

  .project-title {
    font-size: var(--text-2xl);
    font-family: var(--font-display);
    font-weight: var(--weight-regular);
    line-height: var(--leading-tight);
    max-width: 24ch;
    margin-bottom: var(--space-3);
  }

  .project-sub {
    font-size: var(--text-lg);
    color: var(--color-fg-muted);
    font-family: var(--font-display);
  }

  .project-section {
    padding-block: var(--space-8);
    max-width: var(--measure);
  }

  .project-section .block-eyebrow {
    border-top: 0;
    padding-top: 0;
    margin-bottom: var(--space-3);
  }

  .project-section p strong {
    font-weight: var(--weight-medium);
  }

  .project-figure {
    margin: var(--space-6) 0;
  }
  .project-figure img {
    width: 100%;
    height: auto;
    border: 1px solid var(--color-rule);
  }
  .project-figure figcaption {
    margin-top: var(--space-2);
    font-size: var(--text-xs);
    letter-spacing: 0.05em;
    text-transform: uppercase;
    color: var(--color-fg-muted);
  }
</style>
